{
  "name": "hexnut-handle",
  "version": "0.0.1",
  "description": "Basic middleware helper for dealing with connections and messages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Francis Stokes",
  "license": "MIT",
  "dependencies": {
  }
}
